# 이 파일을 복사해 `.streamlit/secrets.toml` 로 저장하고 값을 채우세요.
# 실제 secrets.toml 은 .gitignore 에 의해 git 에 올라가지 않습니다.

[supabase]
url = "https://YOUR_PROJECT.supabase.co"
anon_key = "YOUR_ANON_KEY"
service_role_key = "YOUR_SERVICE_ROLE_KEY"
